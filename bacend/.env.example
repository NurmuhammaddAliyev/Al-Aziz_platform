DB_ENGINE=django.db.backends.postgresql
DB_NAME=al_aziz_platform
DB_USER=postgres
DB_PASSWORD=
DB_HOST=127.0.0.1
DB_PORT=5432

# Application secrets (do NOT commit real values to the repo)
# SECRET_KEY should be set in production environment or CI secrets
SECRET_KEY=

# Initial user passwords for local/test setup (optional - do NOT use real passwords)
INITIAL_ADMIN_PASSWORD=
INITIAL_TEST_USER_PASSWORD=
INITIAL_TEACHER_PASSWORD=

# Script-specific variables
SET_ADMIN1_PASSWORD=
CHECK_ADMIN_PASSWORD=
